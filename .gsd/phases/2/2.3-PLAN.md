---
phase: 2
plan: 3
wave: 2
---

# Plan 2.3: Mock Data Generator

## Objective
Create a mock data generator that mimics the ESP8266 WebSocket server. This allows Phase 3 (PC Hub Processing) to be developed in parallel without waiting for physical hardware assembly.

## Context
- .gsd/ROADMAP.md (Phase 7.2)
- .gsd/STATE.md

## Tasks

<task type="auto">
  <name>Implement Mock Glove Server</name>
  <files>scripts/simulate_glove.py</files>
  <action>
    Create a Python script that hosts a WebSocket server on `ws://localhost:81` (same as ESP8266).
    - Accept connections and periodically (50Hz) send mock CSV strings: `<F1,F2,F3,F4,F5,GX,GY,HR>`
    - Values should gently fluctuate to simulate real sensors.
    - Add keyboard listeners or a simple CLI to trigger specific "gestures" by changing the mock flex values on command.
  </action>
  <verify>python -m py_compile scripts/simulate_glove.py</verify>
  <done>Script successfully starts a WS server and broadcasts correctly formatted mock glove data.</done>
</task>

## Success Criteria
- [ ] Mock script mimics ESP8266 WebSocket behavior.
- [ ] Allows forcing specific gesture vectors for testing.
