---
phase: 5
plan: 3
wave: 2
---

# Plan 5.3: App Rebranding (SYNAPSE)

## Objective
Convert the previous generic UI (which may have been focused on a smartwatch/health app) into the deeply integrated SYNAPSE Bio-Digital Larynx theme (Deep Purple/Cyan).

## Context
- .gsd/SPEC.md
- flutter_app/ (If it exists)

## Tasks

<task type="auto">
  <name>Update App Identity</name>
  <files>
    flutter_app/lib/main.dart
    flutter_app/pubspec.yaml
  </files>
  <action>
    - If `flutter_app` does not exist, leave a `notes/app_rebrand.txt` specifying the Deep Purple/Cyan HEX colors (`#2d004d`, `#00e5ff`) for whenever the UI is built.
    - If it does exist:
      - Rename `MaterialApp` title to "SYNAPSE".
      - Update `ThemeData` to use `Brightness.dark`, `primaryColor` deep purple, and `accentColor` cyan.
  </action>
  <verify>flutter analyze (if applicable)</verify>
  <done>App visually represents the final project identity.</done>
</task>

## Success Criteria
- [ ] UI thematic colors match the Bio-Digital Larynx spec.
